# opsi — todo

- Push `init-marketplace` — nothing is installable until pushed (remote pushes are user-owned); then follow `_review/MIGRATION.md` to disable local dupes, and drop the 3 worktree guards from `~/.claude/settings.json` if enabling `worktree@opsi`.
- Live-test the flow runner against a real `claude -p` from a plain terminal (nested guard blocks it inside a session; how-to in the looper/grind skills' "First run — smoke-test it"; memory: flow-runner-needs-live-test).
- (done) Build the isolation-reviewer agent → `review` plugin — shipped genericized (tenancy model established first, 7 bug classes) in 0031f7a; doc-auditor (docs-vs-code drift audit) came along from the same sweep.
- (done) Extract the git-extras kernel → `git` plugin — `hotfix` skill shipped (test-first, cherry-pick both ways, remotes user-owned) in b53a7df; `release` stays domain-locked (Portainer/build-ID machinery).
- (done) Fold kupimkuham's `feedback` `.todo` processor into flow — shipped as its own `feedback` skill beside `triage-todo` (work-now ledger vs route-later) in 2ece616, plus the `todo-readonly-guard` hook.
- (done) Decide: enable `flow@opsi` in THIS repo? — user said YES (2026-07-18): registered the checkout as a directory marketplace in `.claude/settings.local.json` (gitignored) with `flow@opsi` enabled. From next session the todo-readonly-guard is live — updating `.todo` needs the user to arm with "ALLOW TODO"; unarmed deferrals go to `.todo-inbox`. CLAUDE.md updated.
- (done) make grind and loop worktree aware — shipped in 0.0.5: `--worktree NAME` on both runners, `loop --worktree all`, the committed version-locked `bin/loop`/`bin/grind` wrapper, and worktree confinement. The two sub-questions were ANSWERED rather than built: chats do prepare the commands (the looper/grind/collab skills hand over `bin/loop --worktree ...`), and chat CAN run background shells, but an unattended nested `claude -p --dangerously-skip-permissions` is refused by both `_flowlib.nested_guard` and the harness permission classifier — deliberate, not to be routed around; terminal handoff stays.
- (done) would it be possible for worktree to merge into develop even if that branch is not checked out — yes, answered in 0.0.4 via the pinned `_integration` worktree.
