---
description: Commit — bare imperative single line, auto by default, safe staging (runs the commit skill)
---

Invoke the `commit` skill via the Skill tool and follow its SKILL.md exactly. This wrapper carries no
logic — the skill is the single source of the rules.

Arguments: $ARGUMENTS
