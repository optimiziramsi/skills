---
description: The sanctioned way to change a rule/cap/enforcement — explicit approval, coherence check, re-test, changelog (runs the rules-change skill)
---

Invoke the `rules-change` skill via the Skill tool and follow its SKILL.md steps in order. This
wrapper carries no logic — the skill is the single source.

Arguments: $ARGUMENTS
