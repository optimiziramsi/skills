---
description: >-
  Capture or curate a durable lesson in .agent/lessons/ — one file, tiered + indexed (runs the
  lessons skill)
---

Invoke the `lessons` skill via the Skill tool and follow its SKILL.md steps exactly. This wrapper
carries no logic — the skill is the single source of the steps.

Arguments: $ARGUMENTS
