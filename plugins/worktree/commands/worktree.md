---
description: >-
  Drive a parallel, isolated worktree topic — reserve, plan, execute reviewed slices, land to the
  integration branch (runs the worktree skill)
---

Invoke the `worktree` skill via the Skill tool and follow its SKILL.md steps exactly. This wrapper
carries no logic — the skill is the single source of the protocol.

Arguments: $ARGUMENTS
