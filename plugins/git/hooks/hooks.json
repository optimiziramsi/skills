{
  "description": "git plugin — enforce git safety on Bash git commands (PreToolUse): block push/pull, amend, rebase, reset --hard, --no-verify, and discards of uncommitted work",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/git-guard.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
