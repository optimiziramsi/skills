---
description: Shareable changelog of what this session shipped — themes, decisions, pending (runs the session-summary skill)
---

Invoke the `session-summary` skill via the Skill tool and follow its SKILL.md steps exactly. This
wrapper carries no logic — the skill is the single source of the steps.

Arguments: $ARGUMENTS
