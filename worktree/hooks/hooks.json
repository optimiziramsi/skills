{
  "description": "worktree — keep file edits inside the active git worktree (mitigates claude-code #36182) and flag a worktree-rooted session at start. Kill-switch: WORKTREE_GUARD_DISABLE=1. Self-gates: guards fire only inside a linked worktree.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/worktree/hooks/worktree-detect.sh\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/worktree/hooks/worktree-write-guard.sh\"",
            "timeout": 10
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/worktree/hooks/worktree-bash-guard.sh\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/worktree/hooks/worktree-leak-detector.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
